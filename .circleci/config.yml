version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: make
          command: ./configure && make
      - run:
          name: test
          command: tests/test1.sh
