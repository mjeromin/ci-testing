# automake will generate our Makefile.in for configure

# Tell automake not to expect a GNU project layout
AUTOMAKE_OPTIONS = foreign

# this is our target build
bin_PROGRAMS = helloworld

# this is our source
helloworld_SOURCES = hello-world.c
